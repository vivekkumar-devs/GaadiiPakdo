<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.GaadiiPakdo" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- ✅ FORCE visible status bar -->
        <item name="android:statusBarColor">#F5F7FA</item>

        <!-- ✅ Prevent fullscreen -->
        <item name="android:windowFullscreen">false</item>

        <!-- ✅ Ensure system UI is not drawn behind -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

    </style>

    <style name="Theme.GaadiiPakdo" parent="Base.Theme.GaadiiPakdo" />

</resources>